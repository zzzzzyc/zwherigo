COMPATIBILITY_NOTES = [
    "Lua output aims for semantic compatibility, not byte-identical output.",
    "Condition/action translation is intentionally thin and expands incrementally.",
    "GWC generation requires an injected compiler adapter.",
    "Unknown or not-yet-modeled fields should remain in extras during migration.",
]
